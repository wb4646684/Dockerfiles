#!/usr/bin/env bash

echo "Usage: start-jira.sh [OPTION]"
echo "    -fg                            Start JIRA in the foreground"
echo "    --disable-addons=ADDONS        Start JIRA with ADDONS disabled, where ADDONS is a colon-separated list of addon keys"
echo "    --disable-all-addons           Start JIRA with all user installed addons disabled"